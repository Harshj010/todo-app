<!-- paper_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Background color similar to paper -->
    <solid android:color="@color/paper_color" />
    <!-- Border color to give a textured appearance -->
    <stroke
        android:width="1dp"
        android:color="@color/paper_border_color" />
</shape>
